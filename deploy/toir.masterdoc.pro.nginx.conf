# toir.masterdoc.pro — static landing (HTTPS)

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name toir.masterdoc.pro;

    ssl_certificate /etc/letsencrypt/live/toir.masterdoc.pro/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/toir.masterdoc.pro/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /var/www/toir.masterdoc.pro;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location ~* \.(css|js|svg|png|jpg|jpeg|gif|ico|webp|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}

server {
    listen 80;
    listen [::]:80;
    server_name toir.masterdoc.pro;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/certbot;
        default_type text/plain;
        allow all;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
