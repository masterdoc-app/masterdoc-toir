# A/B-эксперименты лендинга fixaverse.ru

**Статус (2026-06-15):** этап A (`copy`) **остановлен** — трафик нецелевой. Creative AB **остановлен** — невалидный тест. Активен только **Intent H1–H3** (тексты объявлений, не параметры URL). История: [DIRECT_CAMPAIGNS_EXPERIMENT.md](DIRECT_CAMPAIGNS_EXPERIMENT.md) §6.

Документ описывает все варианты лендинга, которые можно тестировать в Яндекс Директе через параметры URL.  
Базовая настройка кампании — в [DIRECT_YANDEX.md](DIRECT_YANDEX.md).

**Реализация:** `landing/ab-variants.js` (реестр текстов) + `landing/main.js` (переключение по `URLSearchParams` при загрузке).

---

## Что не меняется между вариантами

| Элемент | Значение |
|---------|----------|
| Форма, UTM, цель Метрики | `demo_form_submit` |
| Логика отправки заявки | Formspree + `ym(109561586, 'reachGoal', 'demo_form_submit')` |

Все остальное (H1, eyebrow, bullets, подзаголовок, hero, длина страницы) — **независимые параметры URL**.

---

## Сводная таблица параметров

| Параметр | Значения | Дефолт |
|----------|----------|--------|
| `layout` | `full`, `lean` | `full` — вся страница |
| `h1` | `toir`, `uchet` | HTML: «Первая система ТОиР на основе ИИ» |
| `eyebrow` | `concrete`, `none` | «Связанный. Интеллектуальный. Адаптивный.» |
| `bullets` | `pain`, `value` | скрыты |
| `copy` | `toir`, `uchet`, `auto` | универсальный текст (см. §2) |
| `hero` | `trucks`, `copilot` | `hero-bg.jpg` + UI-карточки |

Параметры **комбинируются**:

```
?layout=lean&h1=uchet&bullets=pain&copy=uchet&hero=trucks#demo
```

Невалидный ключ — игнорируется, показывается дефолт.

В заявку Formspree уходят скрытые поля: `ab_layout`, `ab_h1`, `ab_eyebrow`, `ab_bullets`, `ab_copy`, `ab_hero`.

---

## 1. Эксперимент: длина страницы (`?layout=`)

| Ключ | Что видит пользователь |
|------|------------------------|
| *(нет параметра)* / `full` | Полная страница (контроль) |
| `lean` | Hero → блок доверия → форма. Без platform, radar, copilot, killer-more, FAQ, CTA-banner, machines |

### Блок доверия (только `layout=lean`)

- Для заводов и сетей объектов
- On-premise и private cloud — данные в вашем контуре
- Старт без IoT-датчиков — на истории ремонтов и документации

### Примеры URL

```
https://fixaverse.ru/#demo
https://fixaverse.ru/?layout=full#demo
https://fixaverse.ru/?layout=lean#demo
```

---

## 2. Эксперимент: заголовок H1 (`?h1=`)

| Ключ | Текст |
|------|-------|
| *(нет параметра)* | Первая система ТОиР на основе ИИ |
| `toir` | Система ТОиР на основе ИИ |
| `uchet` | Программа учёта ремонтов на предприятии |

```
https://fixaverse.ru/?h1=toir#demo
https://fixaverse.ru/?h1=uchet#demo
```

---

## 3. Эксперимент: eyebrow (`?eyebrow=`)

| Ключ | Текст |
|------|-------|
| *(нет параметра)* | Связанный. Интеллектуальный. Адаптивный. |
| `concrete` | Учёт ремонтов · Copilot в цеху · On-premise |
| `none` | строка скрыта |

```
https://fixaverse.ru/?eyebrow=concrete#demo
https://fixaverse.ru/?eyebrow=none#demo
```

---

## 4. Эксперимент: тезисы под CTA (`?bullets=`)

Список из 3 пунктов под кнопкой «Запросить демо».

| Ключ | Тезисы |
|------|--------|
| `pain` | Простои из‑за потерянного опыта · Excel/PDF · нет памяти по машине |
| `value` | Заказ-наряды · Copilot у станка · память машины |

```
https://fixaverse.ru/?bullets=pain#demo
https://fixaverse.ru/?bullets=value#demo
```

---

## 5. Эксперимент: текст под H1 (`?copy=`)

Переключает абзац `.hero-desc`. Без параметра — **контрольный** универсальный текст.

### Контроль (без `?copy=`)

> Fixaverse — система управления техническим обслуживанием и ремонтами на базе ИИ. Программа для обслуживания оборудования и учёта ремонтов: заказ-наряды, автоматизация ТОиР и copilot для техников в поле.

### Вариант `toir` — «система ТОиР»

**Wordstat (РФ, 30 дней):** `система тоир` — **1 370** показов/мес.

> Fixaverse — система ТОиР для предприятия на базе ИИ. Управление обслуживанием и ремонтом оборудования: планирование, заказ-наряды и память каждой машины.

```
https://fixaverse.ru/?copy=toir#demo
```

### Вариант `uchet` — «программа учёта ремонтов»

**Wordstat:** `программа учета ремонтов` — **388**; `программа для обслуживания оборудования` — **259**.

> Fixaverse — программа учёта ремонтов и обслуживания оборудования. Заказ-наряды, история поломок и документация — без Excel и разрозненных PDF.

```
https://fixaverse.ru/?copy=uchet#demo
```

### Вариант `auto` — «автоматизация + copilot»

**Wordstat:** `автоматизация технического обслуживания и ремонта` — **323**.

> Fixaverse — автоматизация технического обслуживания и ремонта на предприятии. Copilot у станка: диагностика шаг за шагом, фото и отчёт в одном контуре.

```
https://fixaverse.ru/?copy=auto#demo
```

---

## 6. Эксперимент: картинка hero (`?hero=`)

| Ключ | Файл | UI-карточки |
|------|------|-------------|
| *(нет параметра)* / `trucks` | `assets/hero-bg.jpg` | **вкл.** |
| `copilot` | `assets/hero-cmms.webp` | **выкл.** |

```
https://fixaverse.ru/?hero=copilot#demo
```

---

## 7. Рекомендуемая очерёдность тестов

Не менять несколько параметров в одном этапе — не хватит трафика.

### Ссылки в объявлениях Директа vs `#demo` на сайте

- **В объявлениях** — landing **с верха страницы**, без `#demo`: человек сначала видит hero и текст варианта (`?copy=`, `?hero=` и т.д.), потом сам решает перейти к форме.
- **`#demo`** — только для кнопок **внутри** лендинга («Запросить демо»), не для входящего трафика из Директа.

### Creative AB (картинки в объявлении) — **остановлен, невалиден**

Кампания `710704391` **SUSPENDED** с 2026-06-12: все объявления с одним креативом G4 — сравнивать нечего.

Возобновлять только при 2–3 **разных** утверждённых изображениях и **после** чистого трафика по Intent (не параллельно с этапом A).

### Этап A — только `copy` — **остановлен 2026-06-15**

Кампания `710704384` **SUSPENDED**. Причина: трафик нецелевой (автотаргетинг), эксперимент невалиден. Итоги 12–15.06: 14 кликов, ~629 ₽, 0 заявок.

Одинаковый hero, **четыре объявления** (архив настроек):

| Объявление | Ссылка |
|------------|--------|
| A0 (контроль) | `https://fixaverse.ru/` |
| A1 | `?copy=toir` |
| A2 | `?copy=uchet` |
| A3 | `?copy=auto` |

Тексты объявления в Директе — **одинаковые**.

### Этап B — победитель `copy` + `hero` (2 недели)

| Объявление | Ссылка |
|------------|--------|
| B1 | `?copy=<winner>&hero=trucks` |
| B2 | `?copy=<winner>&hero=copilot` |

### Этап C — `layout` при фиксированном copy/hero (2 недели)

| Объявление | Ссылка |
|------------|--------|
| C0 | `?copy=<winner>&hero=<winner>` (full) |
| C1 | `?copy=<winner>&hero=<winner>&layout=lean` |

### Этап D — тезисы по одному (опционально)

При `layout=lean` тестировать отдельно: `h1`, `eyebrow`, `bullets` — по одному параметру за этап.

Пример:

```
https://fixaverse.ru/?layout=lean&copy=toir&bullets=pain#demo
```

---

## 8. Как мерить в Метрике

| Метрика | Где смотреть |
|---------|--------------|
| Клики, CTR | Отчёты Директа по `ad_id` (`utm_content`) |
| Конверсия заявки | Цель `demo_form_submit`, счётчик **109561586** |
| Разбивка по параметрам | Отчёт → Параметры URL → `layout`, `copy`, `hero`, `h1`, `bullets`, `eyebrow` |
| Связка объявление → вариант | `utm_content` + параметры URL |
| Данные в CRM/почте | Поля `ab_*` в письме Formspree |

### Минимум данных

~**30–50 кликов на вариант**, иначе выводы будут шумом. Для `demo_form_submit` лучше **≥5 заявок** суммарно или **≥40 кликов** на вариант.

### Что не смешивать в одном тесте

- Не менять **заголовки объявления** одновременно с параметрами лендинга.
- Не менять **ключевые фразы** между вариантами одного этапа.
- Внутри этапа — **один** изменяемый параметр, остальные дефолт.

---

## 9. Креатив в объявлении vs hero на сайте

| | Где показывается | Файл сейчас |
|---|------------------|-------------|
| Картинка в объявлении | До клика, в выдаче/РСЯ | `landing/assets/ad-direct-1080x1080.png` |
| Hero на лендинге | После клика | `hero-bg.jpg` / `hero-cmms.webp` |

Директ **не меняет** hero на сайте сам — только через `?hero=`.

---

## 10. Чеклист перед запуском эксперимента

- [ ] DNS и HTTPS на fixaverse.ru работают
- [ ] В Метрике цель `demo_form_submit` активна
- [ ] В Директе привязан счётчик 109561586
- [ ] У каждого объявления свой URL с нужными параметрами
- [ ] Тексты и ключи объявлений **одинаковы** внутри одного этапа теста
- [ ] В отчёте Метрики добавлены сегменты по `layout`, `copy`, `hero`, `h1`, `bullets`
- [ ] Тестовая заявка приходит в Formspree с полями `ab_*`

---

## Связанные файлы

| Файл | Назначение |
|------|------------|
| [DIRECT_CAMPAIGNS_EXPERIMENT.md](DIRECT_CAMPAIGNS_EXPERIMENT.md) | Запущенные кампании Copy/Creative AB (ids, объявления) |
| [DIRECT_YANDEX.md](DIRECT_YANDEX.md) | Кампания, ключи, минус-слова |
| [WORDSTAT_TOIR_AUTOMATION_LEADS.md](WORDSTAT_TOIR_AUTOMATION_LEADS.md) | Замер частотности ключей |
| `landing/ab-variants.js` | Реестр текстов и вариантов |
| `landing/main.js` | Логика применения параметров |
| `landing/index.html` | Дефолтный HTML, trust-strip, форма |
